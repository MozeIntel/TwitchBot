namespace TwitchBotApi.Scripting
{
    /*
     * Interface all scripts inherit from.
     * Don't use this directly, inherit from a lower-level interface.
     * Used for internal code.
     */
    public interface IScript
    {
        //Empty interface is empty.
    }
}
